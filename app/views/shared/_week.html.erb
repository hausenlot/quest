<div class="flex justify-between items-center mb-4">
  <h2 class="text-3xl font-bold"><%= Date.today.strftime("%B #{Date.today.beginning_of_week.strftime("%d")} - #{Date.today.end_of_week.strftime("%d")} %Y") %></h2>
</div>

<div class="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg shadow-lg">
  <%= week_calendar events: @quests, class: "w-full" do |date, quests| %>
    <%# I just said f it and let chatgpt cook this one. Then I handle some lame ass tailwind mybad lmao%>
  <% end %>
</div>